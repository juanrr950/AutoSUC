# This is a basic workflow to help you get started with Actions

name: deploy_juanas

# Controls when the action will run. 
on: 
  # Triggers the workflow on push or pull request events but only for the dev branch
  push:
    branches: 
      - main
  
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
    deployment:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy Stage
          uses: fjogeleit/http-request-action@master
          with:
            url: 'http://juanas-as6404t.myasustor.com:19900/api/endpoints/1/docker/containers/autosuc_container/restart'
            method: 'POST'
            bearerToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwidXNlcm5hbWUiOiJhZG1pbiIsInJvbGUiOjEsImV4cCI6MTYxMTE3MTQzNn0.bVJ6lhDnte4B6bptR68I0zBU3JZXMLu5fel_l_btsYM'
